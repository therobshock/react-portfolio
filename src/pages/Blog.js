import React, { Component } from "react";
import { Container } from "../components/Grid";

class Blog extends Component {
  state = {
  };
  render() {
    return (
      <Container fluid>
      </Container>
    );
  }
}

export default Blog;
